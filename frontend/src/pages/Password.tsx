import{useState,type FormEvent}from'react';import{Link,useNavigate}from'react-router-dom';import{Logo}from'../components';import{request}from'../api';
export function ForgotPassword(){const[email,setEmail]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);try{await request('/auth/forgot-password',{method:'POST',auth:false,body:JSON.stringify({email})});setMessage('If an active account uses this email, a password-reset link has been sent.')}finally{setBusy(false)}}return <div className="register-page"><Link to="/"><Logo/></Link><form className="auth-card" onSubmit={submit}><h2>Reset your password</h2><p>Enter your SmartHire email to receive a secure reset link.</p>{message&&<div className="alert success">{message}</div>}<label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></label><button className="button full" disabled={busy}>{busy?'Sending…':'Send reset link'}</button><p className="center"><Link to="/login">Return to login</Link></p></form></div>}
export function ResetPassword(){const token=new URLSearchParams(location.search).get('token')||'',nav=useNavigate();const[password,setPassword]=useState(''),[confirmation,setConfirmation]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(password!==confirmation)return setError('Passwords do not match.');setBusy(true);setError('');try{await request('/auth/reset-password',{method:'POST',auth:false,body:JSON.stringify({token,password})});nav('/login')}catch(e){setError(e instanceof Error?e.message:'Could not reset password')}finally{setBusy(false)}}return <div className="register-page"><Link to="/"><Logo/></Link><form className="auth-card" onSubmit={submit}><h2>Choose a new password</h2><p>This link can only be used once.</p>{error&&<div className="alert error">{error}</div>}<label>New password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={8} required autoComplete="new-password"/></label><label>Confirm password<input type="password" value={confirmation} onChange={e=>setConfirmation(e.target.value)} minLength={8} required autoComplete="new-password"/></label><button className="button full" disabled={busy||!token}>{busy?'Resetting…':'Reset password'}</button></form></div>}
