import{useState,type FormEvent}from'react';import{request}from'../api';import{useAuth}from'../auth';
export function Profile(){const{user}=useAuth();const[message,setMessage]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const values=Object.fromEntries(new FormData(e.currentTarget).entries());await request('/profiles/me',{method:'PATCH',body:JSON.stringify(values)});setMessage('Profile updated successfully.')}return <><div className="page-title"><div><h1>Profile</h1><p>Manage your personal and professional information.</p></div></div><form className="panel form" onSubmit={submit}>{message&&<div className="alert success">{message}</div>}<div className="profile-avatar">{user?.full_name[0]}</div><div className="form-grid"><label>Full name<input name="full_name" defaultValue={user?.full_name}/></label><label>Email<input value={user?.email} disabled/></label>{user?.role==='employer'&&<label>Company name<input name="company_name" defaultValue={user.company_name}/></label>}<label>Location<input name="location" defaultValue={user?.location}/></label>{user?.role==='applicant'&&<label>Professional headline<input name="headline" defaultValue={user.headline}/></label>}</div><button className="button">Save Profile</button></form></>}

